const useEntity = () => {
    // Use context / namespace package / reimplement namespaces via a new useNamespace hook

    //Move reference implementations into hooks and use components as wrappers only
}
